# Example .env for Vocaloid PoC
# Copy this file to ".env" and update values for your environment.

# Path where workspace files (DB, uploaded samples, render_jobs) will be stored
WORKSPACE=/var/www/html/vocaloid_workspace

# LLM endpoint used to translate raw text to CMU ARPABET phonemes
LLM_API_URL=https://api.example-llm.local/v1/generate
# Model identifier to request from the LLM service
LLM_MODEL=phi4:latest

# Optional: API key for the LLM service if required
LLM_API_KEY=REDACTED
